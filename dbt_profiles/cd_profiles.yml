default:
  target: {{DBT_TARGET}}
  outputs:
    {{DBT_TARGET}}:
      type: snowflake
      role: {{SNOWFLAKE_ROLE}}
      warehouse: {{SNOWFLAKE_WAREHOUSE}}
      database: {{SNOWFLAKE_DATABASE}}
      schema: {{SNOWFLAKE_SCHEMA}}
      account: ''
      user: {{SNOWFLAKE_USER}}